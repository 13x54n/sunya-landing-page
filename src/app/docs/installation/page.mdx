export const metadata = {
  title: "Installation | Sunya",
  description: "Install Sunya for static analysis of EVM smart contracts",
}

# Installation

Install Sunya globally to use the CLI across your projects, or run it via npx without installing.

## npm (global)

```bash
npm install -g sunya
```

## npx (no install)

Run Sunya without installing it globally:

```bash
npx sunya scan
```
